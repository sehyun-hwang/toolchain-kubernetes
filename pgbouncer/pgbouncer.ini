[databases]
* = host=${PGHOST} dbname=${PGDATABASE}
​
[pgbouncer]
listen_addr = 127.0.0.1
listen_port = 5432
auth_type = trust
auth_file = userlist.txt
server_tls_sslmode = verify-full
server_tls_ca_file = global-bundle.pem
